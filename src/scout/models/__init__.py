# __init__.py — scout.models subpackage marker.
# Purpose: Groups the Pydantic v2 data models that describe SOV tracking, investigations, recommendations, and blog detection.
# Scope: No runtime logic; importing the package does not eagerly import submodules.
# Consumers: scout/state.py, every node under scout/nodes/, and the db/ layer for serialization.
