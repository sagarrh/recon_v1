# __init__.py — Scout package root marker.
# Purpose: Declares the `scout` namespace so submodules can be imported as `scout.<x>`.
# Scope: No runtime logic; importing the package does not eagerly load submodules.
# Consumers: Every module under scout/ and the top-level run.py entrypoint.
