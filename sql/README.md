# Reference SQL

These files document the original analysis model and are not applied automatically.

Runtime database migrations live in `migrations/` and are packaged under
`src/ai_visibility/resources/migrations/`. The active parameterized Recon report query lives
at `src/aivc/resources/sql/recon_report.sql`.
